import { MaskIdDirective } from './mask-id.directive';

describe('MaskIdDirective', () => {
  it('should create an instance', () => {
    //const directive = new MaskIdDirective();
    //expect(directive).toBeTruthy();
  });
});
