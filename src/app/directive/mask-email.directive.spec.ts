import { MaskEmailDirective } from './mask-email.directive';

describe('MaskEmailDirective', () => {
  it('should create an instance', () => {
    //const directive = new MaskEmailDirective();
    //expect(directive).toBeTruthy();
  });
});
